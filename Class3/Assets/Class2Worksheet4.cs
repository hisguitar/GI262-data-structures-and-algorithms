using UnityEngine;

public class Class2Worksheet4 : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        int hp = 0;
        
        if (hp > 50)
        {
            if (hp > 50)
            {
                Debug.Log("hp is high");
            }
            else
            {
                Debug.Log("hp is medium");
            }
        }
    }
}